{% form_theme form 'bootstrap_5_layout.html.twig' %}
{% extends "admin.html.twig" %}
{% block body %}
<div class="container my-5">
    <div class="col-lg-6 col-md-10  col-12 mx-auto shadow-lg">
        <div class="border px-3 py-1 rounded-top rounded-top-3">
            <p class="text-center h3 fw-bold">Reaffectation</p>
        </div>
        <div class="border p-3 rounded-bottom rounded-bottom-3">
            {{form_start(form)}}
                {{form_widget(form)}}         
            {{form_end(form)}}
        </div>
       
    </div>
</div>
{% endblock %}