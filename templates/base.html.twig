<!DOCTYPE html>
<html  class="h-100">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text></svg>">
        {# Run `composer require symfony/webpack-encore-bundle` to start using Symfony UX #}
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}

        {% block javascripts %}
            {{ encore_entry_script_tags('app') }}
            <script src="{{asset("DataTables/datatables.js")}}"></script>
        {% endblock %}
    </head>
    <body class="d-flex flex-column h-100">
        <header class="bg-primary">
            <div class="container-fluid ">
                <p class="display-1 text-center fw-bold"> Observatoire citoyen des élections</p>
            </div>
        </header>
        <div class="container-fluid bg-secondary py-2">
            <ul class="nav justify-content-center" style="height:50px;">
                <li class="nav-item">
                    <a class="nav-link text-white btn btn-secondary" href="{{path("app_home")}}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white btn btn-secondary" href="{{path("app_resultat")}}">Resultats</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white btn btn-secondary" href="{{path("app_espaceCi")}}">Espace Citoyen</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link text-white btn btn-secondary" href="{{path("app_apropos")}}">A propos</a>
                </li>
            </ul>
        </div>
        {% block body %}{% endblock %}
        <footer class="footer mt-auto py-3 bg-dark">
            <div class="container">
                <span class="text-light">Place sticky footer content here.</span>
            </div>
        </footer>
    </body>
</html>
